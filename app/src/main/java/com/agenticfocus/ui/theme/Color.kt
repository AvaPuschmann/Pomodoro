package com.agenticfocus.ui.theme

import androidx.compose.ui.graphics.Color

val TomatoRed = Color(0xFFE53935)
val DarkBackground = Color(0xFF0D0D0D)
val GlassWhite = Color.White.copy(alpha = 0.15f)
val TextWhite = Color.White
val SubtleWhite = Color.White.copy(alpha = 0.6f)
val ArcBackground = Color.White.copy(alpha = 0.2f)
val LedCore  = Color(0xFFFFF8E1)   // warm cream — bright LED center
val LedGlow  = Color(0xFFFFD54F)   // light amber — inner glow
val LedHalo  = Color(0xFFFFB300)   // golden amber — outer halo
